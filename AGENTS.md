# Agent Notes

- For database-backed features, do not mark work complete until the required Supabase migration has been applied or the blocker is stated plainly.
- Verify the rendered app after UI changes. If browser automation fails, say that directly instead of implying visual verification happened.
- When local dev uses the development login shortcut, sign in with the full Supabase email to test production data paths.
